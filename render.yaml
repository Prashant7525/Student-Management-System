services:
  - type: web
    name: student-management-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn student_management.api.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health